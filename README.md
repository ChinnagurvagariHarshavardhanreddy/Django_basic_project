'In windows You want to set upt the Django by using'
'create virtual environment'
python -m venv venv
'Activate the virtual environment'
venv\Scripts\activate
'Install Django frame'
pip install Django
Django-admin startproject project_name
'then after set start app'
py manage.py startapp app_name
"set app_name in setting.py in Installed Apps"
'then run server by '
py mange.py runserver
